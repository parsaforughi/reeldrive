# همان railway.toml — فقط اگر در Railway مسیر config جدا می‌گذارید
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m dashboard"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
